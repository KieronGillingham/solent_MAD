<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <EditTextPreference
        android:defaultValue="50.914"
        android:key="lat"
        android:summary="Latitude to set the map to."
        android:title="Latitude" />
    <EditTextPreference
        android:defaultValue="-1.333"
        android:key="lon"
        android:summary="Longitude to set the map to."
        android:title="Longitude" />
    <EditTextPreference
        android:defaultValue="16"
        android:key="zoom"
        android:summary="Zoom level to show the map at initially."
        android:title="Default Zoom" />
    <CheckBoxPreference
        android:defaultValue="true"
        android:key="example_ckb"
        android:summary="An example of a checkbox preference."
        android:title="Example Checkbox Preference" />
    <ListPreference
        android:entries="@array/mapType"
        android:entryValues="@array/mapTypeValues"
        android:key="map_type"
        android:summary="Choose what map you would like to be displayed."
        android:title="Map Type" />
</PreferenceScreen>

